import { defineConfig } from "vite";
// import { resolve } from "path";

export default defineConfig({
    // root: ".",
    // build: {
    //     rollupOptions: {
    //         input: {
    //             intro: resolve(__dirname, "src/01-intro/index.html"),
    //         },
    //     },
    // },
});
