#lang sicp

(define (is_even n)
  (= (remainder n 2) 0))

(define (fast_expt a b n)
  (cond ((= n 0) 1)
        ((is_even n) (fast_expt a (* b b) (/ n 2)))
        (else (fast_expt (* a b) b (dec n)))))

(fast_expr 3.0 3)